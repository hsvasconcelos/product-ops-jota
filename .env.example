# Copie para .env e preencha. NÃO commite o .env (já está no .gitignore? confira).
EVOLUTION_API_KEY=troque-por-uma-chave-secreta-qualquer
EVOLUTION_INSTANCE=jota
SERVER_URL=http://localhost:8080

# OpenAI (opcional): sem isto, o bot cai no template determinístico (a demo roda).
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o
